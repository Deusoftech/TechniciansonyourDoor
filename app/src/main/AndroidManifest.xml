<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.techniciansonyourdoor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechniciansOnYourDoor">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Language" />
        <activity android:name=".Languages" />
        <activity android:name=".Learn_More" />
        <activity android:name=".Report_Problem" />
        <activity android:name=".Common_Problems" />
        <activity android:name=".Definition" />
        <activity android:name=".Splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>